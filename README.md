## Install

* install
```
sudo apt-get install alacritty emacs fcitx5 \
                    gestures i3 i3status-rust \
                    neovim picom rofi zsh rime
```
* rime config in local 
* theme config in theme
* set python env in direnv 
```
sudo apt-get install direnv
```

## Options

* options config is polybar in polybar

